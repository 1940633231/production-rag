# ============================================================
# Production RAG - 依赖清单
# 安装: pip install -r requirements.txt
# ============================================================

# ---- Web 框架 ----
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
sse-starlette>=1.8.0          # SSE 流式响应
python-multipart>=0.0.6       # FastAPI 文件上传（UploadFile）

# ---- 数据模型 ----
pydantic>=2.5.0

# ---- 认证 / RBAC ----
PyJWT>=2.8.0                 # JWT 签发与校验
bcrypt>=4.0.0                # 密码哈希

# ---- 摄入：文档加载 ----
beautifulsoup4>=4.12.0        # HTML 解析
pypdf>=4.0.0                  # PDF 文本抽取
python-docx>=1.1.0            # Word(.docx) 文本抽取

# ---- 摄入：分词 ----
jieba>=0.42.1                 # BM25 中文分词

# ---- 向量检索 ----
faiss-cpu>=1.7.4              # FAISS 索引
numpy>=1.24.0

# ---- Embedding / Rerank ----
sentence-transformers>=2.2.0  # BGE 系列模型加载
torch>=2.0.0                  # sentence-transformers 依赖

# ---- 生成（LLM） ----
dashscope>=1.14.0             # 阿里云 Qwen DashScope SDK

# ---- 持久化（MySQL，可选） ----
pymysql>=1.1.0                # MySQL 驱动
dbutils>=3.1.0                # 连接池
elasticsearch>=8.9.0,<9.0.0  # Elasticsearch 客户端库（9.x 与 ES 8.x 服务端协议不兼容）
pymilvus>=2.3.0              # Milvus 客户端库

# ---- Token 计数（可选） ----
tiktoken>=0.5.0               # cl100k_base 近似计数

# ---- 评估 ----
scikit-learn>=1.3.0           # 评估指标（Precision/MRR/NDCG 等可复用）
pandas>=2.0.0                 # C-MTEB 评测数据读取（parquet）
pyarrow>=14.0.0               # parquet 文件格式支持（pandas 读取 C-MTEB 数据依赖）

# ---- 测试 ----
pytest>=7.4.0
pytest-cov>=4.1.0

# ---- 可观测性（可选，后续接入） ----
# prometheus-client>=0.17.0
# opentelemetry-sdk>=1.20.0
